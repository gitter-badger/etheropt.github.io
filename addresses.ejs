<ul class="nav navbar-nav navbar-right">
  <li class="dropdown">
    <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><%= addresses[selectedAddr].addr.substring(0,12) %>... <span class="badge"><%= bundle.utility.weiToEth(addresses[selectedAddr].balance) %> eth</span> <span class="caret"></span></a>
    <ul class="dropdown-menu">
      <% addresses.forEach(function(address, i, array) { %>
        <li><a href="#" onclick="bundle.Main.selectAddress(<%= i%>);"><%= address.addr %> <span class="badge"><%= bundle.utility.weiToEth(address.balance) %> eth</span></a></li>
      <% }); %>
      <li>
        <a onclick="bundle.Main.shapeshift_click(this, event);" href="https://shapeshift.io/shifty.html?destination=<%= addresses[selectedAddr].addr%>&amp;output=ETH&amp;amount=">Deposit with ShapeShift.io</a>
      </li>
      <li><a href="#" data-toggle="modal" data-target="#address_modal">Add existing account</a></li>
      <li><a href="#" onclick="bundle.Main.createAddress()">Create new account</a></li>
      <li><a href="#" onclick="bundle.Main.showPrivateKey()">Export private key</a></li>
      <li><a href="#" onclick="bundle.Main.deleteAddress()">Forget current account</a></li>
      <li><a href="#" onclick="bundle.Main.logout()">Forget all accounts</a></li>
    </ul>
  </li>
</ul>
