<ul class="nav navbar-nav navbar-right">
  <li class="dropdown">
    <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><%= addresses[selectedAddr].addr.substring(0,12) %>... <span class="badge"><%= bundle.utility.weiToEth(addresses[selectedAddr].balance) %> eth</span> <span class="caret"></span></a>
    <ul class="dropdown-menu">
      <% addresses.forEach(function(address, i, array) { %>
        <li><a href="#" onclick="bundle.Main.selectAddress(<%= i%>);"><%= address.addr %> <span class="badge"><%= bundle.utility.weiToEth(address.balance) %> eth</span></a></li>
      <% }); %>
      <li><a href="#" data-toggle="modal" data-target="#address_modal">Add address</a></li>
    </ul>
  </li>
</ul>
